---
import Headline from '~/components/ui/Headline.astro';
import WidgetWrapper from '~/components/ui/WidgetWrapper.astro';
import Button from '~/components/ui/Button.astro';
import Image from '~/components/common/Image.astro';
import type { Testimonials as Props } from '~/types';

const {
  title = '',
  subtitle = '',
  tagline = '',
  testimonials = [],
  callToAction,

  id,
  isDark = false,
  classes = {},
  bg = await Astro.slots.render('bg'),
} = Astro.props;
---

<WidgetWrapper id={id} isDark={isDark} containerClass={`max-w-6xl mx-auto ${classes?.container ?? ''}`} bg={bg}>
  <Headline title={title} subtitle={subtitle} tagline={tagline} />

  <div class="grid sm:grid-cols-2 lg:grid-cols-3 gap-6">
    {
      testimonials &&
        testimonials.map(({ title: cardTitle, testimonial, name, job, image }) => (
          <figure class="group flex flex-col justify-between p-6 md:p-8 bg-white border border-gray-100 rounded-xl shadow-sm hover:shadow-xl hover:scale-[1.02] transition-all duration-300 dark:bg-slate-800/50 dark:border-slate-700/50">
            {/* Quote icon */}
            <svg
              class="w-10 h-10 text-primary/20 dark:text-primary/30 mb-4 group-hover:text-primary/40 transition-colors duration-300"
              aria-hidden="true"
              xmlns="http://www.w3.org/2000/svg"
              fill="currentColor"
              viewBox="0 0 18 14"
            >
              <path d="M6 0H2a2 2 0 0 0-2 2v4a2 2 0 0 0 2 2h4v1a3 3 0 0 1-3 3H2a1 1 0 0 0 0 2h1a5.006 5.006 0 0 0 5-5V2a2 2 0 0 0-2-2Zm10 0h-4a2 2 0 0 0-2 2v4a2 2 0 0 0 2 2h4v1a3 3 0 0 1-3 3h-1a1 1 0 0 0 0 2h1a5.006 5.006 0 0 0 5-5V2a2 2 0 0 0-2-2Z" />
            </svg>

            <blockquote class="flex-grow">
              {cardTitle && <h3 class="text-lg font-semibold text-gray-900 dark:text-white mb-2">{cardTitle}</h3>}
              {testimonial && <p class="text-muted dark:text-slate-400 leading-relaxed">{testimonial}</p>}
            </blockquote>

            <figcaption class="flex items-center mt-6 pt-4 border-t border-gray-100 dark:border-slate-700/50">
              {image && (
                <div class="flex-shrink-0">
                  {typeof image === 'string' ? (
                    <Fragment set:html={image} />
                  ) : (
                    <Image
                      class="w-10 h-10 rounded-full object-cover ring-2 ring-orange-100 dark:ring-orange-900/30"
                      width={40}
                      height={40}
                      widths={[40, 80]}
                      layout="fixed"
                      {...image}
                    />
                  )}
                </div>
              )}
              <div class={image ? 'ml-3 rtl:ml-0 rtl:mr-3' : ''}>
                {name && <div class="font-semibold text-gray-900 dark:text-white">{name}</div>}
                {job && <div class="text-sm text-muted dark:text-slate-400">{job}</div>}
              </div>
            </figcaption>
          </figure>
        ))
    }
  </div>
  {
    callToAction && (
      <div class="flex justify-center mx-auto w-fit mt-8 md:mt-12 font-medium">
        <Button {...callToAction} />
      </div>
    )
  }
</WidgetWrapper>
